# 实验更新记录 — 2026-06-08

本次更新包含两项独立改动，均已落地代码：
1. **轨迹解码改为 Bézier 曲线参数化**（Displacement → Bézier control points）
2. **Prompt 文本地图语义增强**（消除文本-数值冗余，注入非冗余地图先验）

---

## 更新一：Bézier 曲线轨迹参数化

### 动机

诊断发现：训练 loss（位移空间 SmoothL1，reg_loss≈0.018）与评估指标（位置空间 minADE≈1.63m）处于**不同空间**。解码器逐步输出位移 `dx/dy`，经 `cumsum` 累加成 60 步位置——

- 直接预测 60 个独立坐标点会丢失轨迹连续性（点可能"不合群"）
- 对 cumsum 轨迹施加 pos_loss 会导致**反向 cumsum 梯度放大**（早期步梯度被放大至多 T=60×），训练不稳定（exp9 实测振荡）

### 方案

解码器改为预测 **6 个 Bézier 控制点**（degree-5 曲线），经预计算的 Bézier 基矩阵解码出 60 步位置偏移：

```
agent_repr [BN, H]
  → MLP → ctrl_pts [BN, K, 6, 2]            (12 个参数/mode，相对 anchor)
  → einsum(bezier_basis[60,6], ctrl_pts)    (预计算常数，无梯度)
  → traj_pos [BN, K, 60, 2]                 (C∞ 平滑位置偏移)
  → + anchor → pred_abs                     (无 cumsum)
```

| 性质 | 旧（位移 + cumsum） | 新（Bézier） |
|---|---|---|
| 预测维度/mode | 120 | **12** |
| 轨迹连续性 | cumsum 保证 | **C∞ 保证** |
| Loss 空间 | displacement | **position（与 minADE 一致）** |
| 梯度放大 | pos_loss 最多 60× | **无** |

### 涉及文件
- `simpl/hybrid_llm_model.py`：新增 `_make_bezier_basis()`；`MLPDecoder` 输出控制点 + Bézier 解码；`InteractionDecoder` 移除 cumsum；新增 `n_bezier_ctrl` 参数
- `simpl/av2_llm_loss.py`：reg_loss 改为 position 空间 SmoothL1（`pred_abs = anchor + predicted_trajs`，无 cumsum）；移除 pos_loss / ep_loss
- `simpl/hybrid_loss.py`：移除 pos_weight / endpoint_weight 透传
- `train_hybrid.py`：新增 `--n_bezier_ctrl`（默认 6）；移除 `--pos_weight` / `--endpoint_weight`；验证 loop 移除 cumsum

### 注意
- 需**从头训练**（旧 ckpt 解码器权重语义不兼容，不可 resume）

---

## 更新二：Prompt 文本地图语义增强

### 动机（核心问题诊断）

**LLM 净贡献仅 +0.06m**（Phase I：LLM frozen=1.6855m → fine-tune=1.63m）。根因分析：

> 当前 prompt 文本完全由数值运动学经**硬阈值模板**生成：
> `text = f(trajs_pos, trajs_vel, trajs_ang, lane_graph)`

由**数据处理不等式（Data Processing Inequality）**：文本是数值的下游确定性变换，携带的信息**不可能超过**数值通道。即——

> **当前文本对数值通道在信息论上冗余。LLM 无论如何微调，都没有"新信息"可抽取。这是 +0.06m 的数学必然。**

### 方法：注入非冗余地图语义

只加入**数值 AgentEncoder / LaneEncoder 通道没有的信息**，并删除与数值冗余的文本。

#### 可行性勘察（AV2 sample，40 场景 train+val）

通过官方 `ArgoverseStaticMap` API 勘察可提取语义的覆盖率：

| 语义 | 覆盖率 | 是否非冗余 |
|---|---|---|
| 人行横道存在 | 37/40 (92%) | ✅ 完全不在数值通道 |
| 人行横道距离 | median 12.5m，范围 1.1–97.5m | ✅ 连续可变，有判别力 |
| 可行驶区域 | 40/40 (100%) | ✅ |
| 路口 | 39/40 (98%) | 部分已用 |
| lane_type / 可跨越性 / 邻车道 | — | ❌ 已在数值通道，冗余 |

**AV2 地图边界**（诚实记录）：地图为纯几何/拓扑，**不含**天气、POI、限速、红绿灯状态——这些需外部数据源。

#### `[MAP CONTEXT]` 文本内容（最终设计）

| 类别 | 内容 | 数据来源 |
|---|---|---|
| 人行横道 | 距离 + 横向方位 + 数量 + **行为条件化**语义 | `get_scenario_ped_crossings` |
| VRU | 前方 30m 行人/骑行者计数 | scenario tracks |
| **agent 类型分布** | 50m 内全部 agent 按 10 类细分（超 max_agents 的也计入） | scenario tracks |
| **空间** | 到可行驶区域边缘距离（中央 vs 贴路缘） | `get_scenario_vector_drivable_areas` |
| 路口 | 内部 / 距离 | `is_intersection` lanes |
| 车道线 | focal 左右两侧（含 no marking）+ 黄白虚实语义 | `left/right_mark_type` (15类) |
| 专用道 | BIKE / BUS | `lane_type` |
| 路线拓扑 | fork / merge 距离 | `successors / predecessors` |

**人行横道行为条件化**（按 focal 观测运动状态，无未来泄漏）：

| focal 运动 | 生成语义 |
|---|---|
| stopped | already stopped at/near it - waiting for pedestrians |
| decelerating | decelerating on approach - likely to yield/stop |
| accelerating | accelerating - likely proceeding through before pedestrians |
| constant | may proceed or yield depending on pedestrians |

### 文本前后对比（真实场景 0925ba29）

**旧（仅模板，无地图先验）：**
```
[AFFORDANCE & INTENTION]
Focal Agent Intention: Stopping
Map Affordance: Intersection-Rules-Apply, Lane-Change-Restricted-Left (Solid Line)

[FOCAL DYNAMICS]
Speed: 9.0→0.1m/s (decelerating) | Heading: straight ahead
```

**新（删除冗余 Map Affordance、保留 intention，并新增 `[MAP CONTEXT]`）：**
```
[FOCAL INTENTION]
Focal Agent Intention: Stopping

[MAP CONTEXT]
Pedestrian crossing 3.2m directly ahead (4 crossings ahead) - focal already stopped at/near it - waiting for pedestrians.
Nearby agents within 50m: 14 vehicles, 1 riderless bicycle, 4 static objects, 1 background object (20 total).
Focal ~2.1m from nearest road edge - interior of drivable area.
Focal currently inside an intersection - right-of-way rules apply.
Focal lane left boundary: double solid yellow (oncoming traffic divider, no crossing).
Focal lane right boundary: none (no marking).
Focal lane forks immediately ahead - route ambiguity (turn vs straight).
Focal lane merges with another in ~46m - merging conflict possible.

[FOCAL DYNAMICS]
Speed: 9.0→0.1m/s (decelerating) | Heading: straight ahead
```

注：`Map Affordance` 行被**删除**——它（Intersection-Rules / Lane-Change-Restricted-Solid-Line）已被 `[MAP CONTEXT]` 的路口、车道线虚实/可跨越性语义完全覆盖，属冗余。`Focal Agent Intention`（源于 focal 运动学）予以**保留**。"停车"原因在旧文本无从解释，新文本通过"人行横道 3.2m + 路口"给出了**因果可推理**的语义，且 20 个 nearby agents 远超 max_agents=6，提示数值通道丢弃了大量 agent。

> 注意（归因）：`Focal Agent Intention` 仍与数值通道冗余（源于 focal 运动学），仅作显式化与可读性，不引入新信息。因此 baseline vs enriched 的净增益应归因于 `[MAP CONTEXT]`。

### 涉及文件
- `data_av2/av2_preprocess.py`：新增 12 个地图语义提取函数 + `build_map_context()` 方法；`process()` 生成 `MAP_CONTEXT` 字段存入 pkl
- `data_av2/av2_hybrid_dataset.py`：拆出 `compose_scene_text`（文本生成）+ `_build_scene_text`（tokenize 包装）；结构 `Labels → [FOCAL INTENTION] → [MAP CONTEXT] → [FOCAL DYNAMICS] → [SURROUNDING AGENTS CONTEXT]`；**删除** `Map Affordance`（冗余于 `[MAP CONTEXT]`），保留 `Focal Agent Intention`

### 辅助脚本（验证用，非训练管线）
- `debug/probe_av2_map_semantics.py`：地图语义覆盖率勘察
- `debug/demo_rich_text.py`：新旧 prompt 并排对比（输出 `debug/out_demo_rich_text.txt`，40 场景）

### 注意
- 需在云端**重跑预处理**生成带 `MAP_CONTEXT` 的新 pkl（旧 pkl 无此字段，dataset 会优雅跳过）
- `--max_text_len` 建议设 **512**。实测新富文本 token 长度（40 场景）：median=416, max=480, p99=476，500 已 0% 截断。512 覆盖观测最大值 + 余量；因 dataset 用 `padding="max_length"`（每样本都填充到该长度），不宜过大（600/768 仅增加 padding 算力，无收益）。

---

## 关键对照实验（待跑，论文核心证据）

| 实验 | 文本 | 目的 |
|---|---|---|
| baseline | 旧模板文本 | LLM 净贡献基线（已知 +0.06m） |
| **enriched** | 新富文本 | LLM 净贡献是否显著上升？ |

若 enriched 的 LLM 净贡献明显 > 0.06m，则**同时证明**：
1. 旧瓶颈是文本-数值冗余；
2. LLM 在有真实非冗余信息时确实有用。

这是支撑"LLM 用于轨迹预测"核心论点的关键证据。
