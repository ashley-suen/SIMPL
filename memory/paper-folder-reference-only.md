---
name: paper-folder-reference-only
description: /paper folder is reference papers for ideas only — do not traverse it for code work
metadata:
  type: project
---

The `paper/` folder holds reference papers used **only** for paper-idea inspiration.

**How to apply:** do NOT traverse, read, or grep `paper/` when modifying or
reviewing CODE — it is irrelevant to the implementation and wastes context. The user
will explicitly tell me when a new paper in there should be consulted for reference.
Until then, treat `paper/` as out of scope for all code tasks.
